{
  "name": "district-vets-split-to-elements",
  "version": "1.0.0",
  "description": "On the OneBlink form 'Notification of a low risk Emergency Animal Disease Exclusion', and others, after a user selects a District Vet this code splits the returned values into several contact details elements.",
  "main": "./src/response/index.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc"
  },
  "author": "John Bentley",
  "license": "ISC",
  "dependencies": {
    "@hapi/boom": ">=10.0.1",
    "@oneblink/cli": "^1.3.1",
    "abort-controller": "^3.0.0",
    "node-fetch": "^3.3.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.14.6",
    "npm-check-updates": "^16.7.10",
    "typescript": "^4.9.5"
  }
}
